# Minimal — most config is in .zshrc (macOS default shell)
# Keep only entries needed for rare bash invocation (scripts with #!/bin/bash)

# Composer (still installed)
export PATH="$PATH:$HOME/.composer/vendor/bin"
